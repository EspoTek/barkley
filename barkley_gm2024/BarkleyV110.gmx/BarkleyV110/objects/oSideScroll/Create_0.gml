event_inherited();
x=5;
y=0;
yy=0;
img=10;
spl=80;
//titleform=0 formation moved to Room Start: every object it touches is
//created after oSideScroll, so none of them existed yet. See Other_4.
if (sA("check",mSpace)=0) sA("loop",mSpace);
