if (sOvar(object_index,-1)=1) exit;
global.dialog=1;
oBarkley.move="";
sOvar(object_index,1);
sCinema(oBarkley,"dialog","If we want to find that key, we should probably check everywhere. Every computer, every desk, every cabinet. Leave no stone unturned.",1);
sCinema(oBarkley,"code",cine_0069);
sCinema(oBarkley,"code",cine_0006);
